#include <string>
#include <vector>

using namespace std;

int solution(int n, int k) {
    int answer = 0;

    string ab = to_string(a) + to_string(b);
    int ab = stoi(ab);

    return answer;
}
int main(void){
    printf(solution(10,3));

}